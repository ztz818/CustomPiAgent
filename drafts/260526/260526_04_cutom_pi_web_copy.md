# cutom_pi_web 源码复制记录

## 操作

已将 `resources/pi-web/` 复制到项目根目录：

```text
cutom_pi_web/
```

复制时排除了以下目录：

- `.git`
- `node_modules`
- `.next`

## 校验

- 目标目录存在：`cutom_pi_web/`
- 包名：`@agegr/pi-web`
- 版本：`0.6.11`
- 目标目录大小：约 `1.4M`
- 未复制 `.git`、`node_modules`、`.next`

## 后续开发命令

```bash
cd cutom_pi_web
npm install
npm run dev
```

默认开发端口来自 `package.json`：

```text
30141
```
